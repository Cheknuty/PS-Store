import styled from "styled-components";


export const AppContnet = styled.article`
    transition: all 0.5s ease-in-out;
    position: relative;
    z-index: 0;
    height: 100vh;
`

export const AppWrapper = styled.div`
    background-color: var(--color-stroke);
`